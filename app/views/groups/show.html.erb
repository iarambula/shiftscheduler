<div class="container">
  <div class="row">
    <div class="col-12">
      <p class="lead"><%= @group.name %></p>
      <%= render 'volunteers/list', volunteers: @volunteers, q: @q %>
      <%= link_to 'Groups', groups_path, class: 'btn btn-secondary hidden-print' %>
      <%= link_to 'Events', group_events_path(params[:id]), class: 'btn btn-secondary hidden-print' %>
    </div>
  </div>
</div>